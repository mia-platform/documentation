---
id: auth-flow
title: Auth Flow
sidebar_label: Auth Flow
---

```html
<!--
  1. Basic Auth (not for production)
  2. External IdP
        authenticate user on Frontend application according with rfc draft
        1. create `client` on your IdP for FE application
        2. register IdP Openid Connect keys on envoy api gateway
        3. choose FE application session mode (BFF or Token Mediation)
        4. configure authtool-bff + redis cache
        5. setup public endpoints for authtool-bff
        6. in case of BFF mode -> add token mediation lua script in envoy
        7. add authtool-bff client on FE application
  3. Legacy
-->
```

## Authentication for public applications

When exposing a frontend application from your project, or even by allowing deletegated authorization
from an external client like a CLI (Command-Line Interface)
